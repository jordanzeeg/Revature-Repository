jdk is the java development kit. the tools packages and libraries provided
jre is the runtime environment.
jvm is the virtual machine that translates the bytecode into the program. It allows the system to be platform independent
pointers in java would be unnecessarily complex and undermine the security of the software. 
bootstrap, extension, and application
they're loaded first
instance and static variables use the heap. 
permgen is used for storage of metadata
metaspace is permgen by a different name
when an object is created Memory is allocated. Fields are initialized to their default values.
java passes by reference
native methods use other languages not java
